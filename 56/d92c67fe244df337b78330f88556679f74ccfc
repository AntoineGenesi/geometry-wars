Merge commit: Merge s44l-20: Fix cube tunnel shooting direction + hit detection.

This commit modifies 4 files (+80/-3 lines), spanning 2 surface geometry, 1 documentation, 1 Markdown docs.
This is a merge commit integrating work from s44l-20 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: docs/HUMAN_TEST.md, src/surfaces/MeshSurface.test.ts, src/surfaces/MeshSurface.ts, ...4l-20-cube-tunnel-map-shooting-hit-detection.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
TOPICS: hit-detection,surfaces
