Merge commit: Merge s44r3-09: Fix hit detection offset — align collision with visual enemy position on all maps.

This commit modifies 5 files (+274/-29 lines), spanning 2 core game loop, 1 networking, 1 tests.
This is a merge commit integrating work from s44r3-09 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: align collision with visual enemy position on all maps.

Additional context from the commit body:
Game state: 9 of 11 s44r3 tasks merged. CollisionSystem now uses enemy.mesh.position
(rendered position above surface) instead of enemy.position (on-surface). Fixes "one whole
body away" hit detection offset on Mobius, torus, peanut, and other curved maps.
Remaining: language selector (s44r3-01), verification report (s44r3-04).

# Conflicts:
#	tasks/s44r3-09-hit-detection-offset-mobius-and-other-maps.md

Key files: src/core/CollisionSystem.ts, src/core/GameInstance.ts, src/network-main.ts, ...n-visual-collision-alignment.regression.test.ts, ...9-hit-detection-offset-mobius-and-other-maps.md.
