Bug fix: QR code lobby height — remove auto-scroll with CSS sizing.

This commit modifies 2 files (+22/-1 lines), spanning 1 UI/HUD, 1 task/ops files.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
- Added #lan-qr-container CSS styling to prevent unwanted scrolling
- Set width: 100% for proper parent alignment
- Use flex layout with centered alignment for QR code
- Set overflow: hidden to prevent scrollbars
- Set flex-shrink: 0 to prevent flex compression
- QR code now displays fully visible without triggering parent scroll
