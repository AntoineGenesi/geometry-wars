Feature addition: surface verification CLI runner + HTML report.

This commit modifies 4 files (+1386/-1 lines), spanning 2 TypeScript source, 1 other, 1 task/ops files.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
- tests/surface-verification/run-surface-verification.ts: CLI runner that
  runs all 4 test suites (speed, bullet origin, hit detection, seam traversal)
  on all 13 surfaces, saves JSON + HTML report, takes Puppeteer screenshot.
  DOM/WebGL shims copied verbatim from run-full-grid-traversal.ts pattern.

- tests/surface-verification/SurfaceVerificationReport.ts: Standalone HTML
  report generator with dark theme, per-surface SVG heatmaps (4 types),
  click-to-expand point detail popup, embedded historical runs table.

- package.json: Added 'test:surface-verification' npm script.

Key files: package.json, tasks/s44o-05c-cli-runner-and-report.md, .../SurfaceVerificationReport.ts, .../run-surface-verification.ts.
TOPICS: surfaces
