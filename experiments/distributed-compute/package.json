{
  "name": "geometry-wars-distributed-compute-experiment",
  "version": "0.1.0",
  "description": "Distributed computation architecture experiment for Geometry Wars 3D server",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run tests/",
    "test:watch": "vitest tests/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
