Bug fix: QR lobby height regression — increase lobby list max-height from 100px to 180px.

This commit modifies 1 file (+1/-1 lines), touching 1 UI/HUD file.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
- s44i-04 set max-height: 100px to prevent parent scrollbar
- This was too restrictive: only allowed ~1 row to be visible
- Each row is ~70px (min-height: 50px + padding + margin)
- Increased to 180px to show 2-3 rows without internal scrolling
- Maintains proper layout balance (available space ~359px, used ~340px after fix)
- Fixes acceptance criteria: at least one full row visible without scrolling
