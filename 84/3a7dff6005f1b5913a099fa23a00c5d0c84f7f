Feature addition: add game mode selection to MP lobby host setup.

This commit modifies 4 files (+108/-8 lines), spanning 1 TypeScript source, 1 networking, 1 UI/HUD.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
Host can now select game mode (Waves/King/Sniper/Rainbow/Claustrophobia)
from the LAN host setup section in StartMenu before starting the server.

- StartMenu.ts: added lanSelectedGameMode field, createLanGameModeSelectorHTML()
  method, game mode selector HTML in #lan-host-surface-pick, event listeners,
  pendingLanJoin gameMode field, pass-through to MenuSelection.quickGameMode
- main.ts: pass gameMode URL param when launching network mode
- network-main.ts: read gameMode URL param to pre-select lobby mode on load

Files: src/ui/StartMenu.ts, src/main.ts, src/network-main.ts

Key files: src/main.ts, src/network-main.ts, src/ui/StartMenu.ts, tasks/s44m-15-lobby-menu-missing-sp-options.md.
