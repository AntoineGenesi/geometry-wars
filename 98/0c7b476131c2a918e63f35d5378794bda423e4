Feature addition: PvP kill feed, player damage numbers, multi-kill indicators.

This commit modifies 5 files (+509 lines), spanning 2 networking, 1 game server, 1 UI/HUD.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Key files: server/rooms/GameRoom.ts, src/network-main.ts, src/network/NetworkClient.ts, src/ui/KillFeed.ts, tasks/s44m-09-pvp-kill-feed-damage-numbers.md.
