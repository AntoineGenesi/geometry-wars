Bug fix: wire map size selection through lobby UI to server — StartMenu.ts, NetworkClient.ts, GameRoom.ts.

This commit modifies 6 files (+99/-8 lines), spanning 2 networking, 1 game server, 1 TypeScript source.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
- StartMenu.ts: add map size selector buttons (Normal/Large/Epic) to MP lobby
- NetworkClient.ts: pass mapSize option when joining/creating room
- GameRoom.ts: accept mapSize in onCreate options and apply to surface scaling
- network-main.ts: read mapSize from room state for client-side scaling
- main.ts: minor import addition

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

Key files: server/rooms/GameRoom.ts, src/main.ts, src/network-main.ts, src/network/NetworkClient.ts, src/ui/StartMenu.ts, and others.
TOPICS: multiplayer
