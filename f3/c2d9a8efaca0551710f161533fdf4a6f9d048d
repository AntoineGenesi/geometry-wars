Merge commit: Merge s44h-17: Lives system enhancement — configurable lives, infinite lives toggle.

This commit modifies 8 files (+585/-15 lines), spanning 3 game server, 2 networking, 1 game entities.
This is a merge commit integrating work from s44h-17 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: configurable lives, infinite lives toggle.

Additional context from the commit body:
Resolved merge conflict in network-main.ts: kept both claustrophobia surface
check from s44h-15 AND lives selector UI + lives param in choice string from s44h-17.

Key files: server/rooms/GameRoom.lives-enhancement.test.ts, server/rooms/GameRoom.ts, server/schema/GameState.ts, src/entities/Player.ts, src/network-main.ts, and others.
