Merge commit: Merge s44r3-03: Fix cube map — bevel edge movement blocking + aim verification.

This commit modifies 6 files (+1006/-1 lines), spanning 4 tests, 1 TypeScript source, 1 task/ops files.
This is a merge commit integrating work from s44r3-03 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: bevel edge movement blocking + aim verification.

Additional context from the commit body:
Game state: Cube map was completely broken (5th attempt). Movement fix: geodesic
deflection guard falls back to BVH when path deflects >32°. Aim: verified via
RealGameTestHarness — all 8 directions work on all 4 tested faces, no locking,
no inversion. Both SP and MP paths use MeshWalker.moveFromInput().

Key files: src/movement/MeshWalker.ts, src/test/cube-aim-direction.test.ts, src/test/cube-aim-real-game.test.ts, src/test/cube-bevel-crossing.regression.test.ts, src/test/cube-realistic-sim.test.ts, and others.
TOPICS: aim
