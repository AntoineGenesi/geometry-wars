Bug fix: brighten dashed lines on weapon mastery screen to white.

This commit modifies 3 files (+24/-11 lines), spanning 2 UI/HUD, 1 Markdown docs.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
- Changed skip connection line color from #d4aa40 (dark gold) to #ffffff (white)
- Increased opacity values for better visibility (0.95/0.60/0.25 from 0.85/0.45/0.12)
- Updated test to expect white color instead of golden
- Files: WeaponMasteryScreen.ts, WeaponMasteryScreen.test.ts
