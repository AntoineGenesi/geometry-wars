Feature addition: surface coverage smooth — movement + hit detection tests for 5 surfaces.

This commit modifies 2 files (+377 lines), spanning 1 tests, 1 task/ops files.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
Adds src/test/surface-coverage-smooth.test.ts covering sphere, sphere-tunnel, capsule, pill, peanut.
10 describe blocks (5 surfaces × movement + hit detection). 40 tests pass in 826ms.

Movement: 4×4 grid walk, seam-crossing no-teleport check, on-surface position validation.
Hit detection: same-UV hit, far-UV no-hit, distance symmetry, monotone, bullet threshold.

Game state: s44r4 session. MP surface geometry tests expanding coverage of server-side
collision math (MPRealGameTestHarness). SP/MP gameplay unaffected — test-only change.
