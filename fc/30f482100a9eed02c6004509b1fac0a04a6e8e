Merge commit: Merge s44j-settings-16f: Settings persistence + non-host client display.

This commit modifies 7 files (+481/-1 lines), spanning 2 game server, 2 networking, 1 tests.
This is a merge commit integrating work from s44j-settings-16f into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: .../GameRoom.persistence-client-display.test.ts, server/rooms/GameRoom.ts, server/schema/GameState.ts, src/network-main.ts, src/network/NetworkClient.ts, and others.
