Merge commit: Merge s44j-pvp-13f: PvP end-of-match statistics screen.

This commit modifies 7 files (+571/-16 lines), spanning 2 game server, 2 networking, 2 UI/HUD.
This is a merge commit integrating work from s44j-pvp-13f into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Additional context from the commit body:
Resolved NetworkClient.ts conflict — kept both health/maxHealth (13e) and totalDamageDealt (13f).

Key files: server/rooms/GameRoom.ts, server/schema/GameState.ts, src/network-main.ts, src/network/NetworkClient.ts, src/ui/GameOverScreen.test.ts, and others.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
