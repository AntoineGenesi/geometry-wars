Merge commit: Merge s44p-02: Fix sphere MP camera tilt + stuck player + camera reset — stable camera frame from surface normal.

This commit modifies 2 files (+84/-19 lines), spanning 1 documentation, 1 networking.
This is a merge commit integrating work from s44p-02 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: stable camera frame from surface normal.
