Merge commit: Merge s44r6b-02: Cube hit detection tighter threshold + camera fix on MP respawn.

This commit modifies 5 files (+67/-3 lines), spanning 1 game server, 1 core game loop, 1 TypeScript source.
This is a merge commit integrating work from s44r6b-02 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Additional context from the commit body:
- Cube-specific ENEMY_HIT_WORLD_CUBE = 0.3 (down from 0.4) in GameRoom.ts + CollisionSystem.ts
- Camera snapToFrame() on MP respawn in network-main.ts (matches SP behavior)
- Both SP and MP paths covered

Game state: Cube hit detection was killing player from invisible enemies.
Camera was stuck bottom-up after respawning on opposite cube face.
User testing required (Level 1 verification).

Key files: server/rooms/GameRoom.ts, src/core/CollisionSystem.ts, src/main.ts, src/network-main.ts, ...-hit-detection-still-sensitive-camera-broken.md.
