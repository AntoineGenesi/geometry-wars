Merge commit: Merge s44r2-12: Vote divergence timeout — 3-sec auto-select when players vote different maps.

This commit modifies 6 files (+130/-3 lines), spanning 2 game server, 2 UI/HUD, 1 networking.
This is a merge commit integrating work from s44r2-12 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: 3-sec auto-select when players vote different maps.

Key files: server/rooms/GameRoom.ts, server/schema/GameState.ts, src/network/NetworkClient.ts, src/ui/VotingScreen.test.ts, src/ui/VotingScreen.ts, and others.
