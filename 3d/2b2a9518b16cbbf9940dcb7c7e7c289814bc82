Merge commit: Merge s44p-03: Fix torus MP UV recovery — negate wy in atan2.

This commit modifies 3 files (+25/-4 lines), spanning 1 game server, 1 Markdown docs, 1 tests.
This is a merge commit integrating work from s44p-03 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: negate wy in atan2.
