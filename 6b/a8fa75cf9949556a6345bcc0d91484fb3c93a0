Merge commit: Merge s44r6-18: Worker compaction resilience — state file + hook detection + resume.

This commit modifies 4 files (+203/-5 lines), touching 4 task/ops files files.
This is a merge commit integrating work from s44r6-18 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: state file + hook detection + resume.

Additional context from the commit body:
5-mechanism survival system for workers that get compacted:
1. .claude-worker-state.json in worktree root — phase/hypothesis/files-read/last-action
2. SessionStart hook detects worktree cwd, injects worker-specific recovery
3. PreCompact hook stamps state file with compacted_at timestamp
4. Stronger prompt: MANDATORY checkpointing, COMPACTION SURVIVAL section, CONTEXT BUDGET WARNING
5. --resume flag: sends recovery message to compacted-but-alive tmux worker

Files: .claude/scripts/orchestrator.sh, .claude/agents/implementer.md, .claude/rules/checkpoints.md

Game state: Tooling improvement only — no game logic. All active workers (PvP, docs) unaffected.

Key files: .claude/agents/implementer.md, .claude/rules/checkpoints.md, .claude/scripts/orchestrator.sh, tasks/s44r6-18-worker-compaction-resilience.md.
