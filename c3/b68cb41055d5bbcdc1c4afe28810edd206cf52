Merge commit: Merge s44l-10: Entity culling — hide enemies >90° from player.

This commit modifies 6 files (+427/-10 lines), spanning 2 tests, 2 TypeScript source, 1 core game loop.
This is a merge commit integrating work from s44l-10 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: hide enemies >90° from player.

Key files: src/core/GameLoop.ts, src/rendering/EnemyInstanceManager.test.ts, src/rendering/EnemyInstanceManager.ts, src/rendering/EntityCulling.test.ts, src/rendering/EntityCulling.ts, and others.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
