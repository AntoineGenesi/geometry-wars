Merge commit: Merge s44r4-03: Fix pickup collection — use analytical surface position for player.

This commit modifies 3 files (+294/-9 lines), spanning 1 core game loop, 1 tests, 1 Markdown docs.
This is a merge commit integrating work from s44r4-03 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: use analytical surface position for player.

Additional context from the commit body:
Root cause: GameLoop.ts compared playerWalker.position (tessellated mesh) with
pickup._surfaceWorldPos (analytical surface). On curved surfaces, mesh diverges from
analytical surface by ~0.4 units, exceeding 0.25 pickup collection radius.

Fix: 1-line change in GameLoop.ts line 535 to use getTransform(u,v).position (analytical).
MP code path already correct. 8 regression tests.

Game state: Cube aim fixed (s44r4-01), pill hit detection fixed (s44r4-02),
MP test harness built (s44r4-05). Mobius seam fix in progress (s44r4-04).
