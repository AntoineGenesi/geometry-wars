Merge commit: Merge s44r6b-06: Mobile weapon mastery screen — sticky close button + scroll + auto-dismiss.

This commit modifies 5 files (+111/-2 lines), spanning 2 UI/HUD, 1 other, 1 networking.
This is a merge commit integrating work from s44r6b-06 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: sticky close button + scroll + auto-dismiss.

Additional context from the commit body:
- WeaponMasteryScreen.ts: sticky header, touch-action: pan-y, overflow scrolling
- network-main.ts: auto-dispose overlay on voting→playing transition
- 4 regression tests

Game state: Mobile users couldn't exit weapon mastery screen. Now auto-dismisses on game start.

Key files: .claude-worker-state.json, src/network-main.ts, src/ui/WeaponMasteryScreen.test.ts, src/ui/WeaponMasteryScreen.ts, ...s44r6b-06-mobile-weapon-mastery-screen-stuck.md.
TOPICS: upgrades
