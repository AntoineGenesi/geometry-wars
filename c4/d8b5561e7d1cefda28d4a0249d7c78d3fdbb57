Feature addition: mobile camera 2x more zoomed.

This commit modifies 2 files (+18/-3 lines), spanning 1 TypeScript source, 1 task/ops files.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
- Reduce mobile starting camera distance from 9 to 7.5 (0.5× desktop default)
- Provides requested 2× zoom-in effect for improved mobile visibility
- Value stays above minimum (6) to avoid geometry clipping
- Desktop camera distance unchanged (15 remains default)
- Pinch-to-zoom and wheel zoom still functional

Closes s44l-12
TOPICS: camera
