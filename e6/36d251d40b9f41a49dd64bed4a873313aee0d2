Merge commit: Merge s44r5-05: Fix cross-map entity contamination — clear entities and event listeners on map switch.

This commit modifies 8 files (+631/-12 lines), spanning 3 Markdown docs, 1 core game loop, 1 TypeScript source.
This is a merge commit integrating work from s44r5-05 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: clear entities and event listeners on map switch.

Additional context from the commit body:
Conflict resolution: combined s44r5-05 (enemySpawner.clear, bulletPool.clear) with
s44r5-06 (surface.dispose, game.dispose) in both onNext and onReplay handlers.

Key files: src/core/Game.map-transition.test.ts, src/main.ts, src/network-main.ts, ...-02-cube-mp-aim-broken-all-faces-7th-attempt.md, ...pill-hit-detection-enemies-inside-stationary.md, and others.
