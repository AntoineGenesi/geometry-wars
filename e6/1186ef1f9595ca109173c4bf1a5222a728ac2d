Feature addition: add settable PvP win conditions — kills/survival/score.

This commit modifies 8 files (+351/-4 lines), spanning 4 game server, 2 networking, 1 UI/HUD.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
- Add pvpKillLimit field (1–50, default 10) to GameSettings, GameState, GameRoom
  Replaces hardcoded PVP_KILLS_TO_WIN=10 so host can configure kill threshold
- Implement 'score' win condition in checkGameOver() for PvP mode:
  Game ends when timeLimit expires; highest score wins
- Add pvpKillLimit slider to GameSettingsPanel PvP section
- Sync pvpKillLimit through Colyseus state → NetworkClient → network-main

Files: GameSettings.ts, GameState.ts, GameRoom.ts, GameSettingsPanel.ts,
       NetworkClient.ts, network-main.ts

Tests: server/rooms/GameRoom.pvp-win-conditions.test.ts (27 tests)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

Key files: server/rooms/GameRoom.pvp-win-conditions.test.ts, server/rooms/GameRoom.ts, server/schema/GameState.ts, server/shared/GameSettings.ts, src/network-main.ts, and others.
TOPICS: scoring
