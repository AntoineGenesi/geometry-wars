Feature addition: PvP kill scoring — fractional damage tracking.

This commit modifies 5 files (+125/-31 lines), spanning 2 game server, 1 networking, 1 UI/HUD.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
Kill score now increments by damage/100 on every PvP hit, not just on full
kills. 2 damage = +0.02 kills; full 100-damage kill = +1.00 kill.
Scoreboard and floating name labels updated to show 2 decimal places.

Game state: PvP damage numbers (s44r2-07) working. Kill score was
integer-only (0 until full kill). Now tracks incrementally as damage is
dealt across both PvP and PvPvE modes.

Key files: server/rooms/GameRoom.pvp-kills.test.ts, server/rooms/GameRoom.ts, src/network-main.ts, src/ui/GameOverScreen.ts, tasks/s44r2-09-pvp-kill-scoring-incremental.md.
