Merge commit: Merge s44r6-04: Mobius collision + Tesla damage — on-surface distance fallback.

This commit modifies 7 files (+275/-36 lines), spanning 2 TypeScript source, 1 game server, 1 core game loop.
This is a merge commit integrating work from s44r6-04 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: on-surface distance fallback.

Key files: server/rooms/GameRoom.ts, src/core/CollisionSystem.ts, src/main.ts, src/network-main.ts, src/test/mobius-collision-fallback.test.ts, and others.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
TOPICS: hit-detection,surfaces
