Merge commit: Merge s44r6-03: Rainbow mode overhaul — color cycling + score multiplier.

This commit modifies 7 files (+312/-40 lines), spanning 3 core game loop, 1 networking, 1 TypeScript source.
This is a merge commit integrating work from s44r6-03 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: color cycling + score multiplier.

Key files: src/core/CollisionSystem.ts, src/core/GameLoop.ts, src/core/modes/RainbowMode.ts, src/network-main.ts, src/rendering/EnemyInstanceManager.ts, and others.
