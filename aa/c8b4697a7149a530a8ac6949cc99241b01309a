Feature addition: interactive weapon playground in wiki modal.

This commit modifies 2 files (+1068 lines), touching 2 UI/HUD files.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
Playable weapon demo with 3D scene (400x300) embedded in wiki modal.
10 distinct weapon types with unique visuals, 8 auto-spawning enemies,
DPS/kills/timer stats overlay, damage popups.

- src/ui/WeaponPlayground.ts: Self-contained 3D playground scene
- src/ui/WeaponWiki.ts: "TRY IT" button integration per weapon card
