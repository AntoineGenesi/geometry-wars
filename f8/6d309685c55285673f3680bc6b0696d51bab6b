Merge commit: Merge s44j-11: peanut hit detection worldRotation fix.

This commit modifies 4 files (+181/-3 lines), spanning 1 Markdown docs, 1 surface geometry, 1 tests.
This is a merge commit integrating work from s44j-11 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: DISCOVERIES.md, src/surfaces/PeanutSurface.ts, ...hit-detection-world-rotation.regression.test.ts, tasks/s44j-11-peanut-map-hit-detection-bad.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
