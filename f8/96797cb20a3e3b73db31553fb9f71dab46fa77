Feature addition: PvP damage numbers + kill feed.

This commit modifies 5 files (+252/-1 lines), spanning 2 networking, 1 game server, 1 UI/HUD.
This feature extends the Geometry Wars 3D codebase with new functionality. The implementation follows the project's established patterns and maintains compatibility with both single-player and multiplayer code paths where applicable.

Additional context from the commit body:
- Server broadcasts 'pvp_hit' with actual damage on every PvP bullet hit
- NetworkClient routes 'pvp_hit' to new onPvpHit callback
- onPvpHit shows red damage numbers at victim's world position (actual amount, not hardcoded -1)
- onPvpKill now calls killFeed.addKill() with correct isLocalKill/isLocalDeath flags
- 6 regression tests added

Game state: PvP hit detection fixed in s44r2-06. This adds the missing
damage number display and kill feed for PvP/PvPvE modes.

Key files: server/rooms/GameRoom.ts, src/network-main.ts, src/network/NetworkClient.ts, src/ui/KillFeed.pvp-damage.regression.test.ts, tasks/s44r2-07-pvp-damage-numbers-missing.md.
