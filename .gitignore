node_modules/
dist/
*.log
.vite/
.DS_Store
Thumbs.db

# Test screenshots and artifacts
screenshots/
test-screenshots/
*.png
test-results/
/tmp/
.claude/worktrees/
worktrees/

# Telemetry session files — not committed (use logs/ for local analysis only)
# Commit only aggregate reports in reports/
logs/performance/
logs/game-state/
logs/profiling/
sessions/
v3_LAN_working.tar.gz

# Worker JSONL conversation transcripts — large (10-100MB each), not tracked in git.
# Kept locally in .claude/worker-logs/ for post-mortem analysis and debugging.
.claude/worker-logs/*.jsonl
