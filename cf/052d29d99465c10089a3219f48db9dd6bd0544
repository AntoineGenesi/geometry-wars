Merge commit: Merge s44p-04: Fix torus ghost kills — negate wy in atan2 + regression tests.

This commit modifies 4 files (+193/-38 lines), spanning 1 documentation, 1 tests, 1 game server.
This is a merge commit integrating work from s44p-04 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: negate wy in atan2 + regression tests.

Key files: docs/HUMAN_TEST.md, .../GameRoom.torus-uv-parameterization.test.ts, server/rooms/GameRoom.ts, tasks/s44p-04-fix-hit-detection-ghost-kills.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
TOPICS: regression,surfaces
