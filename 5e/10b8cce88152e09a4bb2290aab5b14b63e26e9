Bug fix: ESC opens exit menu during voting, fix local menu z-index above VotingScreen.

This commit modifies 2 files (+54/-2 lines), spanning 1 networking, 1 task/ops files.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
- During voting phase, ESC/pause shows localMenuEl (z-index 3200) instead of
  pausing the server (PauseMenu at z-index 2000 was invisible behind VotingScreen at 3100)
- Raised localMenuEl z-index from 300 → 3200 so it renders above VotingScreen
- Same fix for mobile pause button handler
- hideLocalMenu() now also clears localMenuEl display

Files: src/network-main.ts
