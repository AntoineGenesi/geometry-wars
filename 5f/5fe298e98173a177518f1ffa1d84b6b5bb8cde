Merge commit: Merge s44c-10: wire map size selection through lobby UI to server.

This commit modifies 6 files (+99/-8 lines), spanning 2 networking, 1 game server, 1 TypeScript source.
This is a merge commit integrating work from s44c-10 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: server/rooms/GameRoom.ts, src/main.ts, src/network-main.ts, src/network/NetworkClient.ts, src/ui/StartMenu.ts, and others.
