Merge commit: Merge s44r6b-07: Mobile portrait UI — responsive layout + name entry + QR lobby fix.

This commit modifies 4 files (+191/-17 lines), spanning 1 other, 1 networking, 1 UI/HUD.
This is a merge commit integrating work from s44r6b-07 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: responsive layout + name entry + QR lobby fix.

Additional context from the commit body:
- StartMenu.ts: CSS media query for portrait mode, smaller buttons, compact layout
- network-main.ts: promptForNameIfNeeded() for mobile name entry
- QR lobby text fix

Game state: iPhone 13 portrait had squished buttons, missing join, overlapping QR text.
User testing required for visual confirmation.

Key files: .claude-worker-state.json, src/network-main.ts, src/ui/StartMenu.ts, .../s44r6b-07-mobile-portrait-ui-squished-lobby.md.
