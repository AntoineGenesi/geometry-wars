Merge commit: Merge s44j-15: loading screen spinner during game load.

This commit modifies 5 files (+103/-12 lines), spanning 1 other, 1 TypeScript source, 1 networking.
This is a merge commit integrating work from s44j-15 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: index.html, src/main.ts, src/network-main.ts, src/ui/GameLoadingOverlay.ts, tasks/s44j-15-loading-screen-spinner-game-load.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
