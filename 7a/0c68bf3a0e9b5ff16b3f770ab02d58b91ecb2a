Merge commit: Merge s44h-01: Peanut map bullet offset regression — bypass worldToSurface round-trip.

This commit modifies 5 files (+110/-76 lines), spanning 2 Markdown docs, 1 documentation, 1 networking.
This is a merge commit integrating work from s44h-01 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: bypass worldToSurface round-trip.

Key files: DISCOVERIES.md, docs/HUMAN_TEST.md, src/network-main.ts, src/surfaces/PeanutSurface.ts, .../s44h-01-peanut-map-bullet-offset-regression.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
