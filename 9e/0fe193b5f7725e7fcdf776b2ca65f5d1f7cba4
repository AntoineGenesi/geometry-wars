Merge commit: Merge s44l-08c: Add companion count to difficulty scaling input.

This commit modifies 4 files (+82/-3 lines), spanning 1 core game loop, 1 TypeScript source, 1 tests.
This is a merge commit integrating work from s44l-08c into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.

Key files: src/core/DifficultyScaling.ts, src/main.ts, src/test/difficulty-rebalance.test.ts, tasks/s44l-08c-companion-count-in-difficulty.md.

Merge commits in this project integrate worker branch results into master after independent review. The review-gate protocol requires a reviewer agent to write a VERIFIED certificate to .claude/state/reviews/<task-slug>.md before any merge is allowed. This protocol was established after session s44r2 merged 4 tasks without review after context compaction. Each merge commit represents a fully verified, atomic unit of work that has been tested and reviewed independently.
