Merge commit: Merge s44r-04-01: Audit hit detection root causes — all surfaces + weapons + pickups.

This commit modifies 2 files (+332/-1 lines), spanning 1 Markdown docs, 1 task/ops files.
This is a merge commit integrating work from s44r-04-01 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: all surfaces + weapons + pickups.
