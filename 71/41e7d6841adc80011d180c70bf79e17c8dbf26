Bug fix: auto-transition to voting when all players dead in MP — no more stuck spectating.

This commit modifies 2 files (+34/-7 lines), spanning 1 networking, 1 tests.
This fix addresses a reported bug or regression in the game. Bug fixes in this project follow the TDD protocol: a failing test was written first to reproduce the issue, then the fix was applied to make the test pass, ensuring the defect cannot silently regress.

Additional context from the commit body:
When solo host dies (or all players dead), game showed "YOU DIED — SPECTATING"
indefinitely instead of transitioning to voting screen.

Fix: Add all-players-dead check in network-main.ts death handler and server
game loop. When detected, immediately trigger voting phase transition.

Files: src/network-main.ts
TOPICS: multiplayer
