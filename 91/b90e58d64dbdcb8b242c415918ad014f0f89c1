Merge commit: Merge s44r6-09: WebGPU probe — verify requestDevice() not just requestAdapter().

This commit modifies 3 files (+167/-3 lines), spanning 1 tests, 1 TypeScript source, 1 task/ops files.
This is a merge commit integrating work from s44r6-09 into the main branch. The merged changes have been independently reviewed and verified before integration, following the project's review-gate protocol.
The merged work covers: verify requestDevice() not just requestAdapter().

Additional context from the commit body:
Root cause: GPUCapabilities.probeWebGPU() only called requestAdapter(). Three.js
also calls requestDevice() which can fail (driver blocklist). Settings showed
"WebGPU detected" but game always fell back to WebGL2.

Fix: probe requestDevice() after adapter. If it fails, webgpu=false (accurate).
3 regression tests. User testing needed to confirm WebGPU activates on hardware.

Game state: 12 tasks merged this session. PvP peanut still in progress (iteration 3).
TOPICS: webgpu
