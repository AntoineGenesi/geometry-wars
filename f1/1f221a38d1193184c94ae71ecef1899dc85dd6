Chore/maintenance: checkpoint investigation findings — pill hit detection s44r4-02.

This commit modifies 1 file (+121 lines), touching 1 Markdown docs file.
This maintenance commit handles housekeeping work such as task tracking, lock files, worker state, or minor configuration updates that keep the development workflow functioning correctly.

Additional context from the commit body:
Deep trace of collision pipeline:
- s44r3-09 formulas are mathematically equivalent to pre-fix on flat surfaces
- PillSurface.getPoint() applyWorldRotation IS present (obsolete DISCOVERIES note)
- Pill scale=1.0 MEDIUM, no scaling mismatch
- All positions confirmed world-space
- Most likely fix: revert player-enemy to enemy.position (on surface) comparison
- Dead ends documented to avoid re-investigation
